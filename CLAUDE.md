@AGENTS.md

## Claude Code notes

- The standing project context — layout, conventions, and the fact that there's no
  build/test/lint to run — lives in `AGENTS.md`, imported above. Edit it there so
  Cursor/Codex/other agents stay in sync; this file just points at it.
- Nothing in `AGENTS.md` carries a `✅ verified <date>` stamp: this repo has no commands
  to run, so that's expected here, not a gap.
